aws_region       = "us-east-1"
aws_profile      = "default"
environment      = "dev"
project_name     = "olist-retail"
data_bucket_name = "replace-with-a-globally-unique-bucket-name"

# Optional. AWS sends a confirmation message after apply.
# notification_email = "data-alerts@example.com"
